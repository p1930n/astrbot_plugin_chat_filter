{
  "enabled": {
    "description": "默认全局启用聊天过滤",
    "type": "bool",
    "default": true
  },
  "default_group_enabled": {
    "description": "未单独配置群的默认启用状态",
    "type": "bool",
    "default": false
  },
  "global_words": {
    "description": "全局词库；空词条、重复词条和超长词条会被忽略",
    "type": "list",
    "default": []
  },
  "case_sensitive": {
    "description": "区分大小写",
    "type": "bool",
    "default": false
  },
  "stop_event": {
    "description": "命中后阻断后续事件处理",
    "type": "bool",
    "default": true
  },
  "warn_user": {
    "description": "命中后提示用户",
    "type": "bool",
    "default": true
  },
  "warning_message": {
    "description": "命中后的安全提示文案，最多保留 200 个字符",
    "type": "string",
    "default": "消息触发聊天过滤策略，请调整后重试。"
  },
  "max_word_count": {
    "description": "每个词库最多词条数，范围 1-5000",
    "type": "int",
    "default": 200
  },
  "max_word_length": {
    "description": "单个词条最大长度，范围 1-512",
    "type": "int",
    "default": 64
  },
  "violation_records_enabled": {
    "description": "命中违禁词后写入 SQLite 审计记录",
    "type": "bool",
    "default": true
  },
  "mute_duration_seconds": {
    "description": "默认禁言时长，单位秒；实际执行前会按最小和最大边界裁剪",
    "type": "int",
    "default": 600
  },
  "mute_min_seconds": {
    "description": "QQ 禁言最小时长边界，单位秒；后续平台动作实现前需按适配器确认",
    "type": "int",
    "default": 60
  },
  "mute_max_seconds": {
    "description": "QQ 禁言最大时长边界，单位秒；默认 30 天，后续平台动作实现前需按适配器确认",
    "type": "int",
    "default": 2592000
  },
  "report_files_enabled": {
    "description": "启用周期违禁词历史文件；当前阶段仅保留配置，发送任务后续实现",
    "type": "bool",
    "default": false
  },
  "default_report_interval_days": {
    "description": "未单独配置群时的默认历史文件推送间隔，单位天",
    "type": "int",
    "default": 7
  }
}
